# Transfer Orbit Design Project Dependencies

# Core scientific computing
numpy>=2.4.0
scipy>=1.17.0
matplotlib>=3.10.0

# Custom library (install in editable mode)
# e2m2e @ file:///C:/Users/ouyan/codes/e2m2e
# Or install with: pip install -e C:\Users\ouyan\codes\e2m2e

# 注册本仓库为可编辑包，使 `from scripts...` 在任意工作目录下可用（Windows / Linux 通用）
-e .
