# 编程记录

2026年2月24日下午，我对该代码进行了重构，做了大量工作。首先，重写了主函数：先创建所需实例，再从师兄的代码中选取一个较优初值，代入我的程序。随后，我实现了微分修正算法，但该部分细节尚未仔细审查，代码仍显混乱，不过已能正常运行，用于轨道外推的动力学计算。动力学方程直接复用了师兄此前提供的代码，我将原函数整体复制到我的类中作为新方法，并在转化为方法时为每个函数添加了 self 参数，使其能在面向对象框架中正常调用，调试时一次通过。

在调试过程中，AI曾建议我使用42维扩展的状态转移矩阵，但未能调通，于是我改用AI推荐的6维外推转移矩阵进行外推，并让其自动执行微分修正。目前我对微分修正的具体机制仍不完全清楚：我只知道它计算了半个周期后的状态，据此生成状态转移矩阵，再根据该状态与矩阵计算修正量，施加于初始状态后重新计算，直至误差收敛。随后，通过轨道外推方法，生成了1000个周期内的轨道状态点（当前设置为1000点）。我还编写了类内的可视化方法，进行了结果可视化。从效果看，当前实现整体尚可，但仍有大量细节需优化，我对代码尚未完全理解。下一步不能随意提交，必须仔细审查。目前这段代码尚不能作为可维护的代码库，后续需逐个拆分函数，反复整理与重构数遍。