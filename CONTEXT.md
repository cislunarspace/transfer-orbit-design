# Transfer Orbit Design

本上下文描述 CR3BP 轨道转换到星历模型时使用的领域语言，确保单条轨道与轨道族批处理的语义一致。

此外，自 ADR-0001 起，本文档同时定义软件界面国际化相关的领域语言。

## Language

**单条轨道**:
一个 CR3BP 周期轨道对象，通常由顶层 `states`、`times` 和 `period` 描述；也可由用户通过索引从 **轨道族** 中明确选取。
_Avoid_: 单个 JSON、种子轨道（除非确实用于延拓）

**轨道族**:
按延拓顺序排列的一组 CR3BP 周期轨道，通常由顶层 `orbits` 列表描述。
_Avoid_: 采样轨道、代表性轨道集

**CR3BP 周期轨道族类型**:

| 族名 | 英文名 | 相关平动点 | 物理特征 |
|------|--------|------------|----------|
| Lyapunov | Lyapunov Family | L1, L2, L3 | 平面周期轨道，沿共线平动点主轴振荡 |
| Halo | Halo Family (北族/南族) | L1, L2 | 三维周期轨道，分北族 (Class I, z 轴正方向) 和南族 (Class II, z 轴负方向)；稳定性指数接近 1 的高振幅区域通常称为近直线晕轨道 (NRHO)；L3 处无经典 Halo 轨道族。 |
| Vertical | Vertical Family | L1–L5 | 垂直方向振荡的周期轨道 |
| Axial | Axial Family | L1–L5 | 沿平动点轴向的周期轨道 |
| Butterfly | Butterfly Family | L1–L2 | 连接两个共线平动点的对称轨道 |
| DRO | Distant Retrograde Orbit | secondary | 围绕次天体的远程逆行轨道 |
| DPO | Direct Prograde Orbit | secondary | 围绕次天体的顺行轨道 |
| SPO | Short Period Orbit | L4, L5 | 三角平动点附近的短周期轨道 |
| LPO | Long Period Orbit | L4, L5 | 三角平动点附近的长周期轨道 |
| Tadpole | Tadpole Orbits | L4, L5 | 围绕单个三角平动点的蝌蚪形轨道 |
| Horseshoe | Horseshoe Orbits | L4–L5 | 跨越两个三角平动点的马蹄形轨道 |
| RO | Resonant Orbit（共振轨道） | 全系统 | 满足 m:n 共振比例的周期轨道，如 3:1（周期是月球周期的 3 倍）、3:2（1.5 倍），通过 `--ratio` 区分 |

**轨道族转换**:
将轨道族内每条轨道逐条独立转换到星历模型；单条转换失败时默认记录该项并继续处理其余轨道，可由用户选择快速失败。
_Avoid_: 沿族递推、只转换里程碑轨道

**星历转换输入文件**:
用户显式提供给转换脚本的 CR3BP 轨道 JSON 文件；脚本不应隐式选择最新文件。
_Avoid_: 最新输出文件、默认输入文件

**轨道族转换结果**:
一次 **轨道族转换** 产生的单个汇总 JSON，包含每条轨道的转换状态、诊断信息和成功时的星历轨迹。
_Avoid_: 每轨道分散输出、只保存成功项

**星历转换方法**:
星历转换工具用于构造并修正星历模型中轨迹表示的算法选择。对于需要分段表示的情况，方法用于修正不同 **patch points** 对应的 **轨道初值**，使每段外推到下一个 patch point 时位置和速度满足连续性要求；对于周期性强、三体摄动影响小且不拆分的轨道，方法可用于构造和修正轨道初值。

当前支持的星历转换方法类型：
- **标准多重打靶** (`standard`)：单层多重打靶修正，直接以完整星历模型为约束进行迭代。
- **两级多重打靶** (`two_level`)：先修正位置使位置约束满足，再进行第二阶段修正使位置和速度同时满足约束（默认方法，GUI 显示为"两阶段法"）。
- **分段打靶拼接** (`segmented`)：对 Halo/NRHO 由算法自动选用，逐段修正并拼接，避免不稳定轨道自由外推发散。

_Avoid_: 星历数据源、目标天体历表、固定不可选算法

**参考历元**:
用户为星历转换显式提供的 UTC 时间基准，用于将 CR3BP 轨道的初始状态、轨道初值或 **patch points** 映射到 J2000 星历时间。相同的 **星历转换输入文件** 在不同参考历元下可能得到不同星历转换结果。
_Avoid_: 隐式默认历元、轨道周期、普通运行时间、只理解为 GUI 启动时间

**待转换轨道**:
星历转换从 **星历转换输入文件** 中读取并准备转换到星历模型的 **单条轨道**。当输入文件包含 **轨道族** 时，轨道族中的每条单条轨道都是一个待转换轨道，并按轨道族转换语义逐条独立转换。
_Avoid_: 输入文件本身、最新输出文件、轨道族整体作为一个不可拆分对象

**轨道初值**:
用于开始轨道传播或数值修正的一组初始状态量，通常包含位置和速度。轨道初值属于 **初始条件** 的一部分；**参考初值** 可以提供轨道初值、周期及其它生成所需信息。
_Avoid_: 参考数据集、完整初始条件、生成结果、轨道族

**工具**:
GUI 中用户可以直接运行的一项操作入口，例如轨道设计、轨道保持、轨道族生成。工具按用户目标命名；其背后的实现方式不属于用户概念。
_Avoid_: 脚本（实现细节）、模块（代码结构）、命令（只描述执行形式）

**工具注册**:
`src/engine/facade_bridge.py` 中 `TOOL_REGISTRY` 字典声明的工具元数据（`ToolSpec`：绑定的 Pydantic Request 模型、facade 方法名、UI 标签、工具说明、启用状态）。工具清单与实现状态从 e2m2e facade 的 `tool_inventory()` 自动派生：已接入的工具启用，e2m2e 已实现但 GUI 未接入、e2m2e 占位的工具灰显并在悬停时说明状态。e2m2e 新增工具时 GUI 清单零改动跟随。GUI 启动时按注册表渲染工具选择器与参数面板。
_Avoid_: 脚本（实现细节）、镜像文件、脚本入口（容易与 `if __name__ == "__main__"` 混淆）

**轨道设计** (`design_orbit`):
新 GUI 的轨道设计工具，经 `TOOL_REGISTRY` 注册并绑定 `DesignOrbitRequest` 模型。用户在参数面板填写轨道类型、振幅、相位、历元等参数，FacadeBridge 调用 e2m2e 算法层的 `design_orbit` 生成 CR3BP 轨道；结果以 JSON+NPZ 双文件落盘并在画布上叠加显示。
_Avoid_: 生成单条轨道（CLI 脚本概念）、脚本

**轨道保持** (`control_orbit`):
新 GUI 的轨道保持工具，经 `TOOL_REGISTRY` 注册并绑定 `ControlOrbitRequest` 模型。以一条轨道 Artifact 的星历数据为输入，调用 e2m2e 算法层的 `station_keeping.control_orbit` 做蒙特卡洛仿真，输出受控星历与机动 Δv 统计；结果写入 `output/ephemeris/`。
_Avoid_: 轨道设计、脚本

**轨道族生成** (`orbit_family_generation`):
新 GUI 的轨道族生成工具，经 `TOOL_REGISTRY` 注册并绑定 e2m2e 公开的 `FamilyGenerationRequest` 模型。GUI 暴露七族下拉（Halo / NRHO / Axial / Lissajous / SPO / LPO / Horseshoe），按族显示对应参数；桥接层走 `Facade.orbit_family_generation`（5.7.1 起响应 `FamilyGenerationResponse` 携带完整轨道成员与状态三元组，软失败保留部分族）。周期族成员只携带初态与周期，桥接层按周期重采样后以 family Artifact（`output/family/`，NPZ 存 `(m, n, 6)` 三维数组）落盘，画布按成员逐条叠加渲染；Lissajous 为拟周期参数采样。范围与默认由上游模型 `valid_ranges` / model_validator 决定。
_Avoid_: 单条轨道设计（那是 `design_orbit`）、批量并发跑多条单轨道

**稳定性分析** (`orbit_stability`):
右键 orbit Artifact 触发的分析动作（无参数面板，不进工具下拉），对选中的 CR3BP 周期轨道调 e2m2e `algorithm/stability.StabilityAnalysis`，输出 Floquet 乘子、稳定性指数（ν₁/ν₂/ν₃/Broucke）、稳定性分类与分岔检测；结果在对话框展示并落盘 `output/stability/` 独立 JSON（不建 Artifact、不进画布）。
_Avoid_: 轨道设计、轨道族生成

**生成单条轨道**:
用户通过手动输入、参考初值或其它起点条件，生成一条 CR3BP 周期轨道。输出是一个 **单条轨道**，可作为绘图、检查、星历转换或转移设计的输入。
_Avoid_: 生成轨道（未说明输出对象类型）、生成种子、生成一个 JSON

**生成轨道族**:
用户通过延拓等方式生成一组按顺序排列的 CR3BP 周期轨道。输出是一个 **轨道族**，可作为绘图、检查或轨道族转换的输入。
_Avoid_: 生成轨道（未说明输出对象类型）、批量生成单条轨道、生成采样轨道

**初始条件**:
用户为生成轨道提供的起点信息，用于确定待生成轨道的初始状态或目标特征。初始条件可以来自手动输入，也可以来自 **参考初值**；当启用参考初值时，与它对应的手动初始条件输入应互斥。
_Avoid_: 只指手动输入、排除参考初值、生成结果

**周期**:
一条 **单条轨道** 在 CR3BP 模型中完成一次重复运动所需的时间。周期可以来自用户输入、**参考初值**、轨道修正结果或输出诊断。周期不同于采样点数、绘图范围或传播时长；当用户需要生成或显示多个周期时，应使用“周期倍数”等表达。
_Avoid_: 采样点数、任意传播时长、周期倍数

**延拓**:
从一条已知轨道、参考初值或已收敛解出发，沿某个轨道族参数方向逐步生成相邻轨道的过程。延拓通常用于 **生成轨道族**；它描述生成方法，不等同于 **轨道族** 本身。延拓方法分为 **自然延拓** 和 **伪弧长延拓** 两种。
_Avoid_: 轨道族本身、批量生成单条轨道、任意多条轨道文件

**自然延拓** (`natural`):
沿参数方向逐步小步推进，每步做微分修正以收敛到新的周期轨道。大部分轨道族的默认延拓方法。
_Avoid_: 伪弧长延拓、单条轨道生成

**伪弧长延拓** (`pseudo_arclength`):
以弧长为延拓参数的 continuation 方法，能够穿越转折点（自然延拓在此处会失败）。Halo 族唯一支持的延拓方法。
_Avoid_: 自然延拓

**修正**:
工具为满足周期性、连续性或目标约束，对初始条件、轨迹或 patch points 做的数值调整。修正是数值方法概念；在具体工具中应通过限定词说明修正对象，例如轨道修正、周期轨道修正或 patch points 修正。
_Avoid_: 所有修正都是同一种算法、优化、失败后的手动改参数

**patch points**:
分段星历转换中用于表示轨迹段之间连接关系，并进行连续性修正的轨迹连接点（GUI 中显示为"拼接点（数量）"）。不是所有星历转换都需要 patch points；对于不拆分的轨道，工具可以只构造和修正轨道初值。
_Avoid_: 所有星历转换都必须有的点、轨道初值、采样点、标注点、目标点

**容差**:
工具用于判断匹配、收敛、连续性或误差是否可接受的数值阈值。容差通常属于 **高级选项**，工具应提供默认值；用户在需要严格筛选、复现实验或排查失败原因时可以调整。
_Avoid_: 必填基础参数、精度保证、结果误差本身

**失败项**:
一次 **轨道族转换** 中，某条 **单条轨道** 未能成功转换时写入 **轨道族转换结果** 的记录。失败项属于转换结果内部的条目；一次转换即使整体完成，也可能包含失败项。
_Avoid_: 失败运行、部分成功、日志错误

**坐标系**:
用于解释轨道状态、转换结果或绘图位置的参考框架。坐标系影响数据含义和显示方式；新 GUI 画布通过 3D/XY/XZ/YZ 投影切换改变显示视角，仅影响展示，不产生新的转换结果。
_Avoid_: 绘图样式、显示单位、主题

**图层**:
**绘图与可视化** 中可独立显示、隐藏或调整样式的一类视觉元素，例如轨道曲线、天体图标、平动点、坐标轴或标注。新 GUI 画布的地月天体与 L1–L5 平动点图层可独立开关。图层只影响展示，不改变轨道数据、计算结果或输出文件中的数值内容。
_Avoid_: 数据筛选、删除数据、坐标系、输出文件内容

**标注**:
**绘图与可视化** 中用于说明轨道、点、事件、参数或诊断结果的文本或符号。新 GUI 画布自动绘制地月天体与 L1–L5 平动点标注。标注可以由工具自动生成，也可以由用户设置；标注可以显示部分 **诊断信息**，但不等同于完整诊断信息。
_Avoid_: 完整诊断信息、日志、数据本身、只指手写文字

**导出图像**:
用户将当前 **绘图与可视化** 结果保存为图像文件的操作。导出的图像文件属于 **输出文件**，但只保存可视化结果，不代表重新生成或修改轨道数据。如果工具提供导出轨道、诊断信息或分析表格，应使用“导出数据”等不同表达，并说明导出的数据类型。若导出图像会覆盖已有文件，应由保存对话框提示用户确认。
_Avoid_: 导出轨道数据、重新生成轨道、修改输入文件

**输出文件**:
GUI 在项目 `output/` 目录下发现并管理的文件，通常由工具运行产生，也可以作为后续工具的输入。输出文件可被浏览、复制路径、打开所在位置或删除。
_Avoid_: 本次运行新文件（过窄）、结果文件（容易排除可作为输入的中间文件）、任意项目文件（超出 GUI 管理边界）

**输入文件**:
用户为某个工具显式选择的文件。输入文件通常来自 GUI 管理的 **输出文件**，也可以来自用户手动指定的路径。工具不得隐式选择“最新文件”来代替用户选择。
_Avoid_: 最新输出文件、默认输入文件、工具自动猜测的文件

**高级选项**:
通常可以保持默认值的参数，主要用于数值调参、诊断或覆盖默认行为。高级选项不是隐藏功能；当用户需要复现实验、调整收敛行为或定位问题时可以修改。工具的基础运行不应依赖用户修改高级选项。
_Avoid_: 必填参数（不应藏入高级选项）、危险选项、专家专属功能

**工具默认值**:
工具提供的内置参数值，用于让工具在常见场景下可以直接运行或作为输入参考。
_Avoid_: 用户保存的默认值、最近一次运行参数

**用户默认值**:
用户在 GUI 中为某个工具保存的参数值；再次打开该工具时优先使用。用户默认值覆盖工具默认值，但不改变工具本身的内置定义。
_Avoid_: 工具内置默认值、出厂默认值、参数模板

**参考数据集 (Reference Catalog)**:
项目提供或用户指定的轨道参考数据集合，包含可供生成工具使用的轨道记录。参考数据集分为两个阶段：原始数据（raw XLSX）经预处理生成归一化参考数据集（normalized catalog），工具默认使用 normalized 数据集。normalized 数据集缺失时，工具可自动从 raw 数据构建（除非用户禁用）。
_Avoid_: 轨道库（那是产物持久化，不是工具输入参考）

**轨道库 (Orbit Catalog)**:
e2m2e 管理的计算产物库（`catalog/` 目录）：每条产物是一份**记录**，含多维分类（族、平动点、Jacobi、振幅、段存在性）、谱系指针与教学标注。GUI 的项目树清单、过滤与谱系都来自轨道库，重启不丢。
_Avoid_: 参考数据集（工具输入参考）、output/ 文件堆

**记录 (Record)**:
轨道库中的一条产物条目，`record_id` 为唯一标识。一条记录可含 CR3BP 段与星历段（双段并存）；轨道族是一条记录（成员参数在记录内部，可提升为独立记录）。
_Avoid_: 文件、Artifact（那是 GUI 侧的内存表示）

**谱系 (Lineage)**:
产物之间的因果指针：轨道保持产物的 `source_record_id` 指向被控轨道，提升成员指向所属族。上游记录被删时谱系**断链**——产物本身仍可用，仅显示降级标记。
_Avoid_: 文件路径依赖、会话内存引用
_Avoid_: 参考目录（容易误解为文件夹目录）、数据库（容易误解为联网或 SQL 数据库）、模板（容易误解为固定输出）、初值（只表示数据集中的单条记录）

**参考初值 (Reference Initial Condition)**:
用户从 **参考数据集** 中选择的一条轨道记录，或按 Jacobi 常数等条件匹配得到的一组初始条件，用作生成工具的起点。启用参考初值后，与它对应的手动初始条件输入应互斥。
_Avoid_: 参考数据集（数据集合层级）、Catalog 种子、生成结果、手动输入值

**参考初值选择方式**:
用户从 **参考数据集** 中确定 **参考初值** 的方法。当前支持按参考记录编号选择和按 Jacobi 常数匹配。
_Avoid_: nearest-neighbor（算法细节）、Seed ID（内部参数名）、自动选择（容易误解为工具替用户决定）

**按参考记录编号选择**:
用户通过参考数据集中的轨道记录编号确定参考初值。适用于用户已经知道目标参考记录编号的情况。CLI 参数为 `--seed-id`（遗留命名，面向用户概念应称"参考记录编号"）。
_Avoid_: Seed ID、种子编号、数据库编号、文件编号

**Jacobi 常数**:
CR3BP 中的守恒量，由位置和速度决定，反映天体的零速度曲面约束。Jacobi 常数用于轨道筛选、**参考初值** 匹配和稳定性分析。
_Avoid_: 能量（相关但不等价）、质量比、引力参数

**按 Jacobi 常数匹配**:
用户输入目标 Jacobi 常数，工具从参考数据集中匹配合适的参考初值。适用于用户以 Jacobi 常数作为轨道筛选依据的情况。
_Avoid_: 最近邻搜索（算法细节）、按 Jacobi 自动生成、Jacobi 种子

**转移设计**:
设计从一个轨道、状态或任务条件到另一个目标的连接或机动方案。转移设计不同于 **星历转换**：前者解决任务连接与设计问题，后者解决动力学模型转换问题。转移设计按起点和终点轨道类型分为多条管线：DRO→GEO、DRO→RO、GEO→DRO 和 LEO→DRO。
_Avoid_: 星历转换（模型转换）、轨道族转换（批量模型转换）、绘图（结果展示）

**网格搜索**:
转移设计的第一阶段。枚举出发时刻与方向参数（α）的网格，对每个网格点传播转移轨迹，检测是否与目标轨道相交以及是否发生碰撞，筛选出 **可行解**。
_Avoid_: NLP 优化（第二阶段）、暴力枚举（算法细节）

**NLP 优化**:
转移设计的第二阶段。在 **网格搜索** 产出的 **可行解** 上做非线性规划优化，以最小化 **Δv** 为目标，收敛后得到 **最优解**。
_Avoid_: 网格搜索（第一阶段）、微分修正（轨道修正概念）

**可行解**:
**网格搜索** 产出的满足基本约束（与目标轨道相交、无碰撞）的候选转移方案。可行解作为 **NLP 优化** 的输入。
_Avoid_: 最优解（优化后的结果）、搜索结果文件

**最优解**:
**NLP 优化** 收敛后得到的 Δv 最小的转移方案。
_Avoid_: 可行解（优化前的候选）

**Δv（速度增量）**:
转移机动所需的速度变化量，通常为出发速度增量与到达速度增量之和。Δv 是衡量转移代价的核心指标，贯穿转移设计的搜索、优化和结果比较。
_Avoid_: 位置变化、加速度、轨道周期

**搜索结果** / **优化结果**:
转移设计管线两阶段分别产出的 **输出文件** 子类型。搜索结果保存 **网格搜索** 的全部 **可行解**；优化结果保存 **NLP 优化** 后的 **最优解** 及其诊断信息。
_Avoid_: 轨道族转换结果、星历转换结果

**GEO（地球静止轨道）**:
转移设计中 DRO→GEO 和 GEO→DRO 管线的端点轨道。GEO 是围绕地球的近似圆形轨道，在 CR3BP 旋转坐标系中以地球为中心建模；不属于 CR3BP 周期轨道族。
_Avoid_: CR3BP 轨道族、轨道族类型表中的族

**LEO（低地球轨道）**:
转移设计中 LEO→DRO 管线的出发端点轨道。LEO 是围绕地球的低高度近似圆形轨道，在 CR3BP 旋转坐标系中以地球为中心建模；不属于 CR3BP 周期轨道族。
_Avoid_: CR3BP 轨道族、轨道族类型表中的族

**绘图与可视化**:
将 **单条轨道**、**轨道族**、转换结果或分析数据展示为图形的工具类别。新 GUI 以内嵌 matplotlib 画布实现：支持 3D/XY/XZ/YZ 投影切换、地月/L1–L5 图层开关与多轨道叠加。静态图像生成称为绘图；交互式查看或动态展示称为可视化。
_Avoid_: 只说绘图（无法覆盖未来交互式查看）、只说可视化（容易弱化静态图输出）

**检查与分析**:
用于验证 **输入文件** 或 **输出文件**、查看数值诊断、比较结果或理解轨道数据特征的工具类别。检查强调是否符合预期；分析强调解释数据特征。
_Avoid_: 只说检查（无法覆盖数据解释）、只说分析（容易弱化验证语义）

**当前选择 Artifact**:
用户在项目树中当前选中的 Artifact（对应 `output/` 下的 **输出文件**）。需要文件输入的工具可把选中 Artifact 作为输入来源；例如轨道保持以选中轨道 Artifact 的星历数据为输入。
_Avoid_: 隐式输入文件、最新文件、后台自动选择的文件

**删除输出文件**:
GUI 中清理 **输出文件** 的文件管理操作。删除输出文件只适用于项目 `output/` 目录下由 GUI 管理的文件；执行前必须显示待删除文件路径并要求用户确认。
_Avoid_: 删除任意用户文件、无确认删除、后台自动清理

**日志**:
GUI 运行工具时显示的文本输出，包括进度、提示、警告和错误信息。新 GUI 的日志面板由 QThread worker 的 log 信号驱动。日志帮助用户理解运行过程，但不一定完整保存为结果的一部分。
_Avoid_: 诊断信息、结果文件、结构化质量指标

**诊断信息**:
工具写入 **输出文件** 或转换结果中的质量指标、收敛信息、失败原因或数值检查结果。诊断信息用于解释计算质量、复查失败原因或比较不同结果。星历转换中的典型诊断信息包括：
- **收敛残差**：数值算法（如多重打靶的牛顿迭代）求解过程中，代数方程组的解范数残差；反映迭代是否满足收敛判据。
- **连续性误差**：修正完成后，逐段重新传播相邻 patch points 之间的弧段，比较传播终点与目标 patch point 的物理位置偏差；反映修正后轨迹在实际动力学模型中的真实连续性。

_Avoid_: 临时日志、界面提示、用户备注

**显示单位**:
GUI 参数输入框中展示和编辑数值所使用的单位。切换显示单位应保持物理量不变，只改变显示数值。
_Avoid_: 标准单位、内部计算单位、改变物理量

**标准单位**:
工具内部接收和计算时使用的单位。GUI 在运行工具前将用户输入按显示单位转换为标准单位；用户通常不需要直接关心标准单位。
_Avoid_: 显示单位、用户偏好单位

**DU (Distance Unit)**:
CR3BP 无量纲化中的距离单位，取地月平均距离 $384400\ \mathrm{km}$。所有无量纲位置量乘以 DU 后得到以千米为单位的物理距离。
_Avoid_: 任意距离、显示单位、无量纲单位本身

**MU (Mass Ratio)**:
CR3BP 中的质量比参数，定义为 $\mu = m_2 / (m_1 + m_2)$，其中 $m_1$ 为地球质量，$m_2$ 为月球质量。本文采用的地月系统 MU 值为 $0.012150585$。MU 决定共线平动点位置、零速度曲面形状以及各类周期轨道的存在区域。
_Avoid_: 引力参数、质量本身、显示单位

**TU (Time Unit)**:
CR3BP 无量纲化中的时间单位，由地月系统引力参数和距离单位共同确定。本文采用的地月系统 TU 对应的物理时长约为 $3.7517$ 天（约 $324\,150$ 秒）。所有无量纲时间量乘以 TU 后得到以天或秒为单位的物理时间。
_Avoid_: 任意时间、显示单位、无量纲单位本身

**VU (Velocity Unit)**:
CR3BP 无量纲化中的速度单位，由 DU 和 TU 共同确定（$\mathrm{VU} = \mathrm{DU} / \mathrm{TU}$）。本文采用的地月系统 VU 约为 $1.024551\ \mathrm{km/s}$。所有无量纲速度量乘以 VU 后得到以 km/s 为单位的物理速度。
_Avoid_: 任意速度、显示单位、无量纲单位本身

**T_MOON**:
月球在 CR3BP 旋转坐标系中的轨道周期，无量纲值为 $2\pi$。T_MOON 用于共振轨道和转移设计中的时间参考。
_Avoid_: 任意周期、TU（基本时间单位）

**设置**:
影响 GUI 整体外观、语言、绘图展示或交互行为的用户偏好。设置跨工具生效，不同于只作用于对应工具参数的 **用户默认值**。
（规划中：新 GUI 尚未实现整体设置界面。）
_Avoid_: 用户默认值、工具参数、单次运行参数

**主题**:
GUI 的整体视觉样式设置，当前支持浅色、深色和跟随系统。主题影响界面显示，不改变工具输入、运行或输出数据。
（规划中：新 GUI 尚未实现主题切换。）
_Avoid_: 绘图样式、输出数据格式、工具参数

**绘图显示设置**:
影响 **绘图与可视化** 结果外观的跨工具 **设置**，例如绘图字号和天体图标缩放。绘图显示设置不改变轨道数据或计算结果。
（规划中：新 GUI 尚未实现。）
_Avoid_: 绘图参数（容易误解为单个工具参数）、轨道数据、计算参数

**工作流链路**:
用户将一个工具产生的 **输出文件** 作为后续工具的 **输入文件**，逐步完成生成、转换、转移设计、绘图、检查与分析等目标的使用过程。
_Avoid_: 工具链（容易强调实现组合）、处理流程（过于泛化）、隐式自动串联

**参数校验**:
GUI 在运行工具前检查用户输入是否满足工具要求的过程。新 GUI 由工具绑定的 Pydantic Request 模型自动校验输入；校验失败时阻止运行，并给出明确原因。
_Avoid_: 后端错误日志、静默失败

**参数范围提示**:
GUI 参数面板中告知用户某参数可填写范围的提示。数值控件框内文本清空时以占位文本显示"可填范围: 最小 ~ 最大（单位）"，悬停控件同样可见；提示随显示单位切换同步换算。范围来自 Pydantic Field 约束（ge/le/gt/lt）；模型缺约束的字段暂用 GUI 临时范围兜底（已提上游 issue 请求补约束）。
_Avoid_: 参数校验、工具说明、错误拦截

**重置参数**:
GUI 参数面板中把当前工具全部参数恢复为默认值的按钮操作（重建面板：模型默认值 + 轨道类型分支默认值 + 默认显示单位）。
_Avoid_: 恢复上次运行参数、用户默认值、撤销单次编辑

**工具说明**:
GUI 中解释 **工具** 用途、输入要求、输出结果和注意事项的用户可见说明文本。工具说明应使用用户概念，而不是实现术语。
_Avoid_: 脚本说明（实现细节）、代码注释、只链接外部文档

## Relationships

- 一个 **轨道族** 包含一条或多条 **单条轨道**。
- **轨道族转换** 对每条 **单条轨道** 执行独立的星历模型转换。
- 每次转换从一个 **星历转换输入文件** 加载 **单条轨道** 或 **轨道族**；**星历转换输入文件** 是 **输入文件** 在星历转换工具中的特例。
- 每次转换使用一个 **星历转换方法** 构造并修正星历模型中的轨迹表示；分段星历转换可构造和修正 **patch points**（GUI 显示为"拼接点"），不分段星历转换可构造和修正 **轨道初值**。
- 每次转换必须由用户提供一个 **参考历元**。
- 一次 **轨道族转换** 产生一个 **轨道族转换结果**。
- 共振比参数 `--ratio` 决定 **RO**（共振轨道）族的具体共振形式（3:1 或 3:2）。
- Halo 族分为北族 (Class I) 和南族 (Class II)，由参数 `--halo-class` 控制。
- GUI 中的 **工具** 经 `TOOL_REGISTRY` 注册（已接入的轨道设计、轨道保持、轨道族生成与右键触发的稳定性分析；其余 e2m2e facade 工具灰显，悬停显示工具说明），按用户目标命名而非按代码目录组织；工具清单与 e2m2e facade 的 `mcp_tools()` 自动对齐。
- **生成单条轨道** 由轨道设计工具实现，**生成轨道族** 由轨道族生成工具实现；工具名称应明确区分 **单条轨道** 与 **轨道族**。
- **生成轨道族** 通过 **延拓** 方法生成；延拓方法分为 **自然延拓** 和 **伪弧长延拓**。
- 星历转换工具围绕 **星历转换输入文件**、**待转换轨道**、**参考历元**、**星历转换方法**、**patch points**、**轨道初值** 和 **失败项** 等概念组织。
- **参考历元** 决定 **待转换轨道** 的初始状态、**轨道初值** 或 **patch points** 如何映射到 J2000 星历时间。
- 转移设计按端点轨道类型分为 DRO→GEO、DRO→RO、GEO→DRO 和 LEO→DRO 四条管线，每条管线包含 **网格搜索** 和 **NLP 优化** 两个阶段。
- **网格搜索** 产出 **可行解**，**NLP 优化** 以 **可行解** 为输入产出 **最优解**，核心优化目标是 **Δv**。
- **Jacobi 常数** 是 CR3BP 守恒量，用于 **参考初值** 匹配和轨道筛选。
- **参考数据集** 从原始 XLSX 数据（raw）预处理为归一化数据集（normalized），工具默认使用 normalized。
- **绘图与可视化** 工具围绕 **坐标系**（3D/XY/XZ/YZ 投影）、**图层**、**标注** 和 **导出图像** 等概念组织。
- **界面语言**、**主题**、绘图字号和天体图标缩放都属于 **设置**（新 GUI 规划中）。

## Example dialogue

> **Dev:** “DRO 轨道族转换时，第二条轨道要复用第一条已收敛的星历解吗？”
> **Domain expert:** “不需要；族内每条轨道逐条独立转换，失败项记录后继续下一条。”

## Flagged ambiguities

- “一个轨道族转换”曾可能表示沿族递推或只转换代表性轨道；已解析为逐条独立转换完整族。
- DRO（表内原始含义 Direct Prograde & Retrograde Orbits）与项目中已有的 Distant Retrograde Orbit 重名；已拆分为 DRO（Distant Retrograde Orbit）和 DPO（Direct Prograde Orbit）两个独立族。
- RO（Resonant Orbit，共振轨道）通过 `--ratio` 参数区分 3:1 和 3:2 共振比例，统一入口为 `generate_ro_orbit.py` 和 `generate_ro_family.py`。ARO/RRO 未成功实现，已从代码库移除。
- SPO（Short Period Orbit）和 LPO（Long Period Orbit）原合并为 Short & Long Period Orbits；已拆分为两个独立族。
- “输入文件”曾可能被理解为工具自动选择最新输出文件；已解析为用户为工具显式选择的文件，**当前选择 Artifact** 作为输入时由用户显式选中。
- Halo 族子类原写 North/South，GUI 显示"北族 (Class I)/南族 (Class II)"；已统一为北族/南族作为主术语，Class I/II 作为同义映射。
- 星历转换方法 two_level 原定义为"粗调→精调"；已修正为"先修正位置，再修正位置+速度"的两阶段递进。
- patch points 在 CONTEXT.md 保持英文主术语，GUI 显示为"拼接点（数量）"作为界面别名。

## UI Language（界面语言）

> 自 ADR-0001 起，软件规划支持中英文界面切换。以下术语定义国际化方案中的用户和翻译概念；实现细节由 ADR-0001 记录。
> 规划中：新 GUI 尚未实现界面语言切换，i18n 模块（`src/app/i18n/`）尚未接入。

**界面语言 (UI Language)**:
软件 GUI 中所有用户可见文本的显示语言。当前支持中文和英文，默认中文；切换界面语言后需重启生效。
（规划中：新 GUI 尚未实现。）
_Avoid_: locale（包含区域格式，本项目仅区分语言）、实时热切换

**源语言 (Source Language)**:
翻译系统中的原始文本语言。本项目源语言为中文，英文版由翻译内容映射获得。
_Avoid_: 英文作为源语言（与常见惯例相反，但符合本项目历史）

**翻译回退 (Translation Fallback)**:
当目标语言的翻译内容缺失时，显示源语言（中文）文本的行为。
_Avoid_: 显示占位符、显示空白、抛出错误

**占位符文本 (Placeholder Text)**:
动态文本中用于运行时填充变量的标记。占位符文本应保留整句语义，避免把一句用户可见文本拆成多个难以翻译的片段。
_Avoid_: 拼接翻译片段、无法独立理解的碎片文本
